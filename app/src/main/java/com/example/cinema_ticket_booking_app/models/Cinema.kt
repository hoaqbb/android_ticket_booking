package com.example.cinema_ticket_booking_app.models

data class Cinema(
      var cinema_id: Int,
      var cinemaName: String,
      var address: String,
      var cinema_img: String,
      var sdt: String
      )
